# Runtime dependencies of REHO.
#
# The authoritative list lives in pyproject.toml; this file installs the package
# itself so the two can never drift apart:
#
#     pip install --extra-index-url https://pypi.ampl.com -r requirements.txt
#
# For a development environment (tests, documentation, linter), use instead:
#
#     pip install --extra-index-url https://pypi.ampl.com -e ".[dev]"

--extra-index-url https://pypi.ampl.com
-e .
